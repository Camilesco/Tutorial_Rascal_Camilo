module BuildAst

import ParseTree;
import Model;

Planning buildPlanning(Tree pt) {
  // placeholder: por ahora solo construimos vacío
  // luego lo llenamos con patrones de tu gramática real
  return planning([]);
}
